// Static fallback data is intentionally empty in production.
// The storefront reads products from the protected API so inventory and pricing
// can never drift from MongoDB.
export const products = [];
